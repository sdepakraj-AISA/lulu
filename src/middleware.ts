// Sprint 2: Middleware placeholder
// TODO: Add authentication middleware logic
export { default } from 'next-auth/middleware';

export const config = {
  matcher: ['/dashboard/:path*', '/onboarding/:path*'],
};

// Made with Bob
